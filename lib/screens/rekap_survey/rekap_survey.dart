import 'package:flutter/material.dart';

class RekapSurveyPage extends StatelessWidget {
  const RekapSurveyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rekap Survey'),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
