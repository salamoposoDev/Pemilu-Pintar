import 'package:flutter/material.dart';

class AddTimSurveyPage extends StatelessWidget {
  const AddTimSurveyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tambahkan Tim Survey'),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
